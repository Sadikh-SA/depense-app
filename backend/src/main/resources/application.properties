spring.application.name=depense-app

# Connexion MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/gestion_depenses?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Moimeme2020$

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Charset (optionnel mais recommandé)
spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci
